name: ai_assistant
description: "An AI Assistant"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.2+2

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  lottie: ^3.1.3 # lottie animations
  hive: ^4.0.0-dev.2 # hive database
  isar_flutter_libs: ^4.0.0-dev.13
  path_provider_android: ^2.0.14
  path_provider: ^2.1.2
  get: ^4.6.6 # for state management
  flutter_animate: ^4.5.2 #custom animation
  animated_text_kit: ^4.2.2
  http: ^1.2.2 #for api call
  flutter_dotenv: ^5.0.2
  dart_openai: ^5.1.0
  cached_network_image: ^3.4.1

  appwrite: ^13.0.0

  google_generative_ai: ^0.4.6

  gallery_saver_plus: ^3.2.1
  share_plus: ^10.1.3

  translator_plus: ^1.0.1
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/lottie/
